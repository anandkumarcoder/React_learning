import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root')); //here react is creating its own dom
root.render(
  // <React.StrictMode>
    <App /> //*  this is function in app.js file which takes HTML element gets rendered on the webpage*/}
    
  // </React.StrictMode>
);


